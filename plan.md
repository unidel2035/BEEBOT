# BEEBOT — План развития проекта

> Дата: 18 марта 2026
> Основан на: [analysis.md](analysis.md)

---

## Фаза 0: Стабилизация (1-2 недели) ✅

Цель: устранить критические баги и уязвимости, привести код в безопасное состояние.

### Безопасность
- [x] JWT-секрет — WEB_PASSWORD обязательный, WEB_SECRET генерируется при отсутствии
- [x] CORS — ограничен до VPS IP и localhost (WEB_CORS_ORIGINS)
- [x] Ошибки API — глобальный обработчик, traceback не утекает
- [x] Rate limiting — slowapi 60/мин

### Критические баги
- [x] Router: `/orders/new` перед `/orders/:id`
- [x] UDS: _request() работает, поллер с cursor-пагинацией и дедупликацией из CRM
- [x] Логист: создаёт заказ в CRM (integram_client передаётся при старте)
- [x] FAISS: нормализация эмбеддингов **до** добавления в индекс (уже реализовано в knowledge_base.py)
- [x] Калькулятор доставки: возвращать `None` при ошибке, не `0` (уже реализовано в logist.py)

### Рефакторинг
- [x] Объединить CRM-константы в `src/crm_constants.py` (уже сделано)
- [x] Убрать дублирующую `setup_admin()` из bot.py — один вызов в main()
- [x] Добавить `asyncio.Lock` для `_order_timeouts` (уже реализовано)
- [x] Обработка истечения Integram-сессии (re-auth) — auto re-auth при 401/403 в integram_api.py

### Чистка репозитория
- [x] Удалить PDF из корня (дубликаты `data/pdfs/`)
- [x] Удалить SESSION_CONTEXT.md, proxy.log, CSV, XLSX
- [x] Удалить `tmp/`
- [x] Обновить `.gitignore`

---

## Фаза 1: Качество кода (2-3 недели) ✅

Цель: покрыть ключевые модули тестами, настроить CI.

### Тестирование
- [x] Unit-тесты для `knowledge_base.py` (поиск, keyword-буст) — 16 тестов
- [x] Unit-тесты для `integram_api.py` (парсинг, маппинг) — 24 теста
- [x] Unit-тесты для агентов (beebot, logist, analyst) — 103 теста
- [x] Integration-тесты для веб-API (FastAPI TestClient) — 22 теста
- [x] E2E smoke-тест: запрос → FAISS → LLM → ответ — 7 тестов
- **Итого: 284 теста собрано (4 ошибки сборки в 3 файлах)**

### CI/CD
- [x] GitHub Actions: lint (ruff) + тесты на каждый PR
- [x] Автоматический деплой на VPS при мерже в main (deploy job в ci.yml, нужны GitHub Secrets)
- [x] Проверка .env переменных при старте (fail fast) — WEB_PASSWORD обязателен

### Код
- [x] Декомпозиция `OrderDetailView.vue` (506→160 строк, 4 компонента)
- [x] Единые Pydantic-модели для бота и веб-API (src/models.py)
- [x] Объединить два CRM-клиента — web/api.py переведён на IntegramClient

---

## Фаза 2: Функциональность (3-4 недели) ✅

Цель: довести до production оформление заказов и доставку.

### Заказы через бота
- [x] Логист → Integram: создание заказа в CRM (create_order + позиции + уведомление)
- [x] Авто-подстановка клиента по Telegram ID (имя, телефон, адрес)
- [x] Валидация телефона (phone_utils.py: normalize, format, validate)
- [x] История диалога для консультанта (последние 5 пар в orchestrator)

### Доставка
- [x] СДЭК API: реальный расчёт стоимости (OAuth + tarifflist + fallback)
- [x] Почта России API: реальный расчёт (tariff.pochta.ru + fallback)
- [x] Авто-трекинг: фоновая проверка + auto-update статуса в CRM (tracker.py)
- [x] Уведомление клиенту при смене статуса (web/notifications.py + bot.py)

### UDS-интеграция
- [x] Починить `_request()` в `uds.py` (retry + backoff + auth)
- [x] Синхронизация заказов UDS → CRM (UDSPoller + cursor-пагинация + дедупликация)
- [x] Синхронизация клиентов UDS → CRM (get_or_create_client_by_phone в sync_uds_transaction)

### Веб-панель
- [x] Пагинация для заказов, клиентов и товаров (page/per_page в API)
- [x] Поиск и фильтры на страницах списков (search param + фильтры по статусу/источнику)
- [x] Экспорт в CSV (/api/export/orders, /api/export/clients, /api/export/products)
- [x] Уведомления в панели (SSE /api/events — order_status, order_tracking)

---

## Фаза 3: Исправление расхождений (1-2 недели)

Цель: устранить конфликты фронтенд↔бэкенд, синхронизировать модули.

### Критические исправления (блокируют пользователей)
- [ ] `POST /api/orders` — создание заказа через веб-панель (сейчас 404)
- [ ] Фронтенд: обновить под пагинированный API (`response.data.items` вместо `response.data`)
- [ ] Фронтенд: компонент пагинации для всех списков
- [ ] `formatDate()` — парсинг DD.MM.YYYY формата из CRM

### Синхронизация уведомлений
- [ ] Смена статуса через бота → SSE в веб-панель
- [ ] Смена статуса через веб-панель → уведомление пчеловоду в Telegram
- [ ] Единый модуль уведомлений (объединить bot.py notify + web/notifications.py)

### Консистентность данных
- [ ] Завершить миграцию всех модулей на `crm_constants.py`
- [ ] Нормализация телефона при создании/обновлении клиента в CRM
- [ ] Единый формат дат (ISO-8601 в API, форматирование на фронтенде)

### Тесты
- [ ] Исправить 4 ошибки сборки тестов
- [ ] Тесты на пагинацию, поиск, экспорт CSV
- [ ] Тесты на SSE-эндпоинт

---

## Фаза 4: Масштабирование (1-2 месяца)

Цель: расширить возможности бота и аналитику.

### Режимы «Голос Улья»
- [ ] 5 стилей ответов (Наставник, Практик, Селекционер, Зимовщик, Эколог)
- [ ] Переключение по команде или кнопке
- [ ] Индивидуальные системные промпты для каждого стиля

### Режим «Осмотр улья»
- [ ] Диалоговый квест: серия уточняющих вопросов
- [ ] Дерево решений для диагностики
- [ ] Рекомендации на основе ответов

### Аналитика
- [ ] Расширенные отчёты в веб-панели
- [ ] Прогнозирование спроса (какие товары фасовать)
- [ ] Сезонная аналитика (что продаётся лучше и когда)
- [ ] ABC-анализ клиентов

### База знаний
- [ ] YouTube Data API v3 — комментарии к видео (реальные Q&A)
- [ ] Автоматическое добавление новых видео
- [ ] FAQ из частых вопросов в боте
- [ ] Динамический keyword-буст из каталога товаров CRM

---

## Фаза 5: Инфраструктура (параллельно)

### Мониторинг
- [ ] Structured logging (JSON) для всех модулей
- [ ] Prometheus-метрики (время ответа, ошибки, запросы)
- [ ] Алерт в Telegram при падении бота или веб-сервера
- [ ] Healthcheck endpoint для Docker

### Бэкапы
- [ ] Ежедневный бэкап CRM через Integram API
- [ ] Бэкап FAISS-индекса при пересборке
- [ ] Хранение бэкапов на hive (не только VPS)

### Оптимизация
- [ ] Кэширование справочников CRM (TTL 5 минут)
- [ ] Connection pooling для HTTP-клиентов
- [ ] Lazy loading для FAISS/sentence-transformers
- [ ] Уменьшение Docker-образа (multi-stage + slim base)

---

## Дорожная карта

```
Март 2026          Апрель 2026         Май 2026           Июнь 2026
──────────────────┬───────────────────┬──────────────────┬──────────────
Фазы 0-2 ✅       │ Фаза 3            │ Фаза 4           │ Фаза 4
Стабилизация      │ Исправление       │ Масштабирование  │ (продолжение)
Тесты + CI        │ расхождений       │                  │
Функциональность  │                   │                  │
                  │ POST /orders      │ Голос Улья       │ Аналитика
                  │ Пагинация UI      │ Осмотр улья      │ Прогнозы
                  │ Уведомления sync  │ YouTube API      │ ABC-клиенты
                  │ Формат дат        │ FAQ              │
──────────────────┴───────────────────┴──────────────────┴──────────────
                     Фаза 5: Мониторинг, бэкапы, оптимизация (параллельно)
```

---

*Анализ проблем: [analysis.md](analysis.md)*
*Архитектурные диаграммы: [docs/architecture.md](docs/architecture.md)*
