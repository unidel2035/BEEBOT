"""Агент «Логист» — диалог с клиентом для оформления заказа.

Собирает ФИО, адрес доставки, предпочтительный способ доставки.
Рассчитывает стоимость доставки через СДЭК / Почту России.

Статус: планируется (заглушка для будущей реализации).
"""


class LogistAgent:
    """Агент для сбора данных о доставке и расчёта стоимости."""

    async def collect_shipping_info(self, chat_id: int) -> dict:
        """Запустить диалог сбора адреса и ФИО клиента."""
        raise NotImplementedError("LogistAgent не реализован")
